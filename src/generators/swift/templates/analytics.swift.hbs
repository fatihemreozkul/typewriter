/**
 * {{autogeneratedFileWarning}}
 */

import Foundation
import Xcore

extension AppAnalyticsEvent {
    {{#each tracks}}
    {{#if functionDescription}}
    /// {{functionDescription}}
    {{/if}}
    {{#if properties.length}}
    public static func {{functionSignature functionName properties false}} -> Self {
        {{indent "        " (propertiesDictionary properties)}}
        return .init(name: "{{rawEventName}}", properties: properties)
    }
    {{else}}
    public static var {{functionName}}: Self {
        .init(name: "{{rawEventName}}")
    }
    {{/if}}

    {{/each}}
}